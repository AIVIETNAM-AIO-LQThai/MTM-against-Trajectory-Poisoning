# Tests
Group 1 must eventually cover dataset validity, trajectory boundaries, RTG, normalization, sequence alignment, padding/loss masking, future-token causality, tiny-subset overfit, evaluation smoke test, and seed reproducibility.
