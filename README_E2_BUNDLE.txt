E2 CSDPC sequence-length fingerprint bundle

This ZIP is repository-relative. Extract it directly into the repository root.

Files
-----

configs/postfinal_controls/e2_csdpc_sequence_length_fingerprint.json
docs/e2_csdpc_sequence_length_protocol.md
scripts/audit_e2_csdpc_sequence_length_fingerprint.py
tests/test_e2_csdpc_sequence_length_fingerprint.py

The experiment is diagnostic only:
- no poisoned dataset
- no candidate generation
- no learner training
- no Group-2 gate changes

Use the Group-2 environment (`g2-cql`) for the actual run.
