[project]
name = "mtm-dt-poisoning"
version = "0.0.0"
description = "Offline-RL security research scaffold for DT, MTM, and trajectory poisoning experiments."
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
