# Configurations

Each method has one simple subfolder. Current Group 1 config:
`configs/dt/walker2d_medium_clean.yaml`.
Future method folders are scaffolded only; leave them unused until their research phase begins.
