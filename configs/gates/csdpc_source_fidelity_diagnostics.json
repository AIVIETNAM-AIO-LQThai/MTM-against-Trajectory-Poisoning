{
  "experiment": "CSDPC_SOURCE_FIDELITY_DIAGNOSTICS",
  "status": "DIAGNOSTIC_ONLY",
  "dataset": "walker2d-medium-v2",
  "canonical_attack_is_unchanged": true,
  "num_clusters": 8,
  "attack_seeds": [0, 1, 2],

  "canonical_reference": {
    "feature_preprocessing": "none",
    "sequence_length": 5,
    "respect_episode_boundaries": true
  },

  "diagnostic_variants": [
    {
      "id": "D0_CANONICAL",
      "feature_preprocessing": "none",
      "sequence_length": 5,
      "respect_episode_boundaries": true
    },
    {
      "id": "D1_LITERAL_L_PLUS_ONE",
      "feature_preprocessing": "none",
      "sequence_length": 6,
      "respect_episode_boundaries": true
    },
    {
      "id": "D2_ZSCORE_PER_DIMENSION",
      "feature_preprocessing": "zscore_per_dimension",
      "sequence_length": 5,
      "respect_episode_boundaries": true
    }
  ],

  "metrics": [
    "raw_distinct_pattern_count",
    "deduplicated_distinct_pattern_count",
    "distinct_pattern_reduction_fraction",
    "dedup_affected_window_fraction",
    "average_deduplicated_pattern_length",
    "adjacent_same_cluster_fraction",
    "cluster_occupancy"
  ],

  "rules": [
    "No poisoned dataset is generated by this diagnostic.",
    "No learner is trained by this diagnostic.",
    "No CQL or DT performance is used to choose a variant.",
    "The canonical no-scaling length-5 reproduction remains unchanged.",
    "Alternative interpretations remain separately named diagnostics.",
    "A diagnostic result cannot retrospectively replace the canonical Gate-B result."
  ]
}