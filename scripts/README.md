# Scripts
User-facing entry points. Prefer method-agnostic commands where practical: `train.py` should dispatch from `method.name` in the config rather than requiring unrelated training scripts for every method.
