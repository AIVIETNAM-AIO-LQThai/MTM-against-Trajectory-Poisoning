# Data
- `raw/` — immutable source benchmark files.
- `clean/` — deterministic clean derivatives.
- `poisoned/` — poisoned copies created only in later groups.
- `metadata/` — hashes, manifests, normalization statistics, audit outputs.
Never modify files in `raw/`.
